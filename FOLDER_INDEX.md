# 项目根目录文件夹索引

## 架构说明
程序入口层，包含唯一入口文件 main.go。
负责加载配置、创建 HTTP 服务、注册信号处理实现优雅关闭。
通过 internal/ 子包实现全部业务逻辑，自身仅承担组装与生命周期管理职责。

## 文件清单

### main.go
- **地位**: 程序唯一入口点
- **功能**: 加载环境变量配置、创建 HTTP 服务（默认 0.0.0.0:9100）、捕获 SIGINT/SIGTERM 实现优雅关闭（10 秒超时）
- **依赖**: context, fmt, log, net/http, os, os/signal, syscall, time, internal/config, internal/server
- **被依赖**: 无（顶层入口）

### fly.toml
- **地位**: Fly.io 部署配置
- **功能**: 定义应用名称、区域、端口映射等部署参数
- **依赖**: 无
- **被依赖**: Fly.io 部署平台

---
⚠️ **自指声明**: 当本文件夹内容变化时（新增/删除/重命名文件），请更新此索引
